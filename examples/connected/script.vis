bounds(0, 500).

//backgroundImage("examples\connected\triangles.svg").

node(N) ->
    image(N, "examples\connected\spinning.gif"),
    dimensions(N, 10, 10).

node(N1), node(N2) ->
    distance(N1, N2, 10).

edge(N1, N2, E) ->
    line(N1, N2),
    distance(N1, N2, 15),
//    shape(E, ellipse),
//    enclosed(N1, E),
//    enclosed(N2, E),
    behind([N1, N2], N1),
    behind([N1, N2], N2).
//    before([N1, N2], E).

//edge(E1), edge(E2) ->
//    distance(E1, E2, ">", 10).

